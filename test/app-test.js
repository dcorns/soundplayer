'use strict';

var expect = require('chai').expect;
var List = require('../app.js');

describe('app tests', function(){
  it ('has a server function',function(){
    expect(true).to.be.true;
  });
});